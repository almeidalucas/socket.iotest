import React from 'react';

const TeamVisitor = ({team_visitor}) => {
  return(
    <div className="team">
      <span>1</span>
      <p>{team_visitor}</p>
    </div>
  )
};

export default TeamVisitor;